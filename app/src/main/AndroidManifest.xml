<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zachklipp.composeviewtreerepro">

  <application
      android:allowBackup="true"
      android:icon="@drawable/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@drawable/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.ComposeViewTreeRepro">
    <activity
        android:name=".FakeNavigationActivity"
        android:label="@string/app_name"
        android:theme="@style/Theme.ComposeViewTreeRepro.NoActionBar">
      <!-- Uncomment this and comment out the filter below to run the fake nav app. -->
      <!--<intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>-->
    </activity>
    <activity
        android:name=".JetpackNavActivity"
        android:label="@string/app_name"
        android:theme="@style/Theme.ComposeViewTreeRepro.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>